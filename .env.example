# Optional: Google Places API Key for real search results
# Without this, the app uses simulated demo data
GOOGLE_PLACES_API_KEY=

# Optional: Maximum results per region (default: 200)
MAX_PER_REGION=200
